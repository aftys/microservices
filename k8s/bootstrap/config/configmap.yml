apiVersion: v1
kind: ConfigMap
metadata:
  name: config-service-config
data:
  application.properties: |
    spring.cloud.config.server.git.uri=https://github.com/aftys/config-server.git
    server.port=8888